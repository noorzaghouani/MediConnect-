<!-- ADMIN PROFILE MODAL PARTIAL -->
<div id="adminProfileModal" class="modal-overlay">
    <div class="modal-card">
        <!-- Header -->
        <div class="modal-header">
            <div class="modal-title">
                <div class="title-icon"><i class="fas fa-user-shield"></i></div>
                <span>Éditer Profil & Sécurité</span>
            </div>
            <button type="button" class="btn-close" onclick="closeAdminProfile()">
                <i class="fas fa-times"></i>
            </button>
        </div>

        <!-- Body -->
        <div class="modal-body">
            <form action="{{ path('app_admin_update_profile') }}" method="POST">
                <input type="hidden" name="_token" value="{{ csrf_token('admin_update_profile') }}">
                
                <!-- Section: Personal Info -->
                <div class="section-title">
                    <i class="fas fa-user"></i> Informations Personnelles
                </div>

                <div class="form-group">
                    <label class="form-label" for="admin_nom">Nom</label>
                    <input type="text" id="admin_nom" name="nom" class="form-input" value="{{ app.user.nom }}">
                </div>

                <div class="form-group">
                    <label class="form-label" for="admin_prenom">Prénom</label>
                    <input type="text" id="admin_prenom" name="prenom" class="form-input" value="{{ app.user.prenom }}">
                </div>

                <div class="form-group">
                    <label class="form-label" for="admin_telephone">Téléphone</label>
                    <input type="text" id="admin_telephone" name="telephone" class="form-input" value="{{ app.user.telephone }}">
                </div>

                <!-- Section: Security -->
                <div class="section-title">
                    <i class="fas fa-lock"></i> Sécurité
                </div>

                <div class="form-group">
                    <label class="form-label" for="admin_current_password">Mot de passe actuel</label>
                    <input type="password" id="admin_current_password" name="current_password" class="form-input" placeholder="••••••••">
                </div>

                <div class="form-group">
                    <label class="form-label" for="admin_new_password">Nouveau mot de passe</label>
                    <input type="password" id="admin_new_password" name="new_password" class="form-input" placeholder="••••••••">
                </div>

                <div class="form-group">
                    <label class="form-label" for="admin_confirm_password">Confirmer le nouveau mot de passe</label>
                    <input type="password" id="admin_confirm_password" name="confirm_password" class="form-input" placeholder="••••••••">
                </div>

                <!-- Footer Actions -->
                <div class="modal-footer-buttons">
                    <button type="button" class="btn-cancel" onclick="closeAdminProfile()">Annuler</button>
                    <button type="submit" class="btn-save">
                        <i class="fas fa-save icon-margin-right"></i> Enregistrer
                    </button>
                </div>

            </form>
        </div>
    </div>
</div>
