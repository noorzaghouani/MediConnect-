<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mes Patients - MediConnect</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('css/medecin/medecin_dashboard.css') }}">
</head>
<body>
    <!-- Sidebar -->
    <aside class="sidebar">
        <div class="logo-section">
            <div class="logo-icon">
                ⚕️
            </div>
            <div class="logo-text">MediConnect</div>
        </div>

        <div class="doctor-profile">
            <div class="doctor-avatar">
                <i class="fas fa-user-md"></i>
            </div>
            <div class="doctor-name">Dr. {{ app.user.prenom }} {{ app.user.nom }}</div>
            <div class="doctor-specialty">{{ app.user.specialite.nom|default('Médecin généraliste') }}</div>
        </div>

        <nav>
            <ul class="nav-menu">
                <li class="nav-item">
                    <a href="{{ path('app_medecin_dashboard') }}" class="nav-link" id="nav-appointments">
                        <i class="fas fa-calendar-check"></i>
                        <span>Mes Rendez-vous</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="#" class="nav-link" id="nav-availability">
                        <i class="fas fa-calendar-alt"></i>
                        <span>Mes Disponibilités</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_medecin_patients') }}" class="nav-link active" id="nav-patients">
                        <i class="fas fa-users"></i>
                        <span>Mes Patients</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="#" class="nav-link" id="nav-prescriptions" onclick="alert('Module Ordonnances bientôt disponible')">
                        <i class="fas fa-file-prescription"></i>
                        <span>Ordonnances</span>
                    </a>
                </li>
            </ul>
        </nav>
    </aside>

    <main class="main-content">
        <header class="header">
            <div class="header-left">
                <h1>Mes Patients</h1>
                <p>Gérez vos patients et accédez à leur historique médical.</p>
            </div>
            <div class="header-right">
                <a href="{{ path('app_medecin_dashboard') }}" class="header-btn btn-primary">
                    <i class="fas fa-arrow-left"></i>
                    Dashboard
                </a>
            </div>
        </header>

        {% for message in app.flashes('success') %}
            <div class="alert alert-success mt-3 mb-4">
                <i class="fas fa-check-circle"></i> {{ message }}
            </div>
        {% endfor %}

        <div class="panel">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="bg-light">
                        <tr>
                            <th class="py-3 ps-4">Patient</th>
                            <th class="py-3">Contact</th>
                            <th class="py-3">Date de naissance</th>
                            <th class="py-3 text-end pe-4">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% if patients is empty %}
                            <tr>
                                <td colspan="4" class="text-center py-5 text-muted">
                                    <i class="fas fa-users fa-2x mb-3" style="color: #cbd5e1;"></i>
                                    <p class="mb-0">Aucun patient trouvé.</p>
                                </td>
                            </tr>
                        {% else %}
                            {% for patient in patients %}
                                <tr>
                                    <td class="ps-4">
                                        <div class="d-flex align-items-center">
                                            <div class="rounded-circle d-flex align-items-center justify-content-center me-3" style="width: 40px; height: 40px; background: #f0fdfa; color: #0d9488;">
                                                <i class="fas fa-user"></i>
                                            </div>
                                            <div>
                                                <div class="fw-bold">{{ patient.nom|upper }} {{ patient.prenom }}</div>
                                                <div class="small text-muted">{{ patient.genre|default('N/A') }}</div>
                                            </div>
                                        </div>
                                    </td>
                                    <td>
                                        <div class="small mb-1"><i class="fas fa-phone me-1 text-muted"></i> {{ patient.telephone|default('N/A') }}</div>
                                        <div class="small"><i class="fas fa-envelope me-1 text-muted"></i> {{ patient.email }}</div>
                                    </td>
                                    <td>
                                        {{ patient.dateNaissance ? patient.dateNaissance|date('d/m/Y') : 'N/A' }}
                                    </td>
                                    <td class="text-end pe-4">
                                        <a href="{{ path('app_dossier_show', {'id': patient.id}) }}" class="action-btn btn-view" style="display: inline-flex; text-decoration: none;">
                                            <i class="fas fa-folder-open"></i> Dossier
                                        </a>
                                    </td>
                                </tr>
                            {% endfor %}
                        {% endif %}
                    </tbody>
                </table>
            </div>
        </div>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
